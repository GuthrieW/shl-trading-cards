import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles((theme) => ({
  root: {},
  appBar: {},
  headerLogoContainer: {},
  headerLogo: {},
  footer: {
    marginTop: '5px',
    width: '100%',
    position: 'fixed',
    bottom: 0,
    zIndex: 1,
  },
}))

export default useStyles
